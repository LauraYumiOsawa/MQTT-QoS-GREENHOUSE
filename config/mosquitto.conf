# Mosquitto configuration for the QoS greenhouse demo
listener 1883
allow_anonymous true

# Persistent sessions (required for QoS 1 and 2 offline message queuing)
persistence true
persistence_location /mosquitto/data/

log_dest stdout
